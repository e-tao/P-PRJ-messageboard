const db = {
    host: "host IP",
    user: "username",
    password: "password",
    database: "database name",
    connectionLimit: 5,
};
  
  
  const config = {};
  
  config.db = db;
  
  module.exports = config;